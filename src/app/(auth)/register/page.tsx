import { RegisterForm } from '@/components/auth/register-form';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: 'Sign Up - Defence Brats',
  description: 'Create your Defence Brats account and join the defence community network.',
};