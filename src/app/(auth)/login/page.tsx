import { LoginForm } from '@/components/auth/login-form';

export default function LoginPage() {
  return <LoginForm />;
}

export const metadata = {
  title: 'Sign In - Defence Brats',
  description: 'Sign in to your Defence Brats account to connect with the defence community.',
};